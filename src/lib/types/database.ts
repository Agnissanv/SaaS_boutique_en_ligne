/**
 * Types générés par Supabase — placeholder en attendant la génération réelle.
 *
 * Une fois le projet Supabase créé et la migration `supabase/migrations/0001_init.sql`
 * appliquée, régénérer avec :
 *
 *   npx supabase gen types typescript --project-id <PROJECT_ID> > src/lib/types/database.ts
 *
 * En attendant, on type en `any` pour ne pas bloquer le développement.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
